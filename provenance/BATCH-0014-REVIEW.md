# BATCH-0014 source-aligned review

OLP-0033 non-enumerability.tex: source SHA-256 `4471def29c2da48dcb01635adf692cd0bc89cb6885636db149f4b2d86ff4e9fa`; target 12926 bytes, SHA-256 `07ea8011875408b83e30949595a3dd658294f055a02616482bf437dcdae250e4`. OLP-0034 reduction.tex: source SHA-256 `0d6d2e3280698668b837cef6dcf3134b86d2a4b48831ae516e6e33e42898c79a`; target 8357 bytes, SHA-256 `2b3bb97a798c179f5013fb47bc6ac203a5eba8ea8f91b5cea4a370ec863ec84a`. Respectively 27 and 22 aligned blocks; strict audit `work/batch-0014-audit.json`, SHA-256 `aa5086e28201e67060a904eb5ef015f26349811d6599945029166f1fb06b8290`, passes every structural check after rejecting initial formula-order, command-inventory and lexical-token discrepancies. The wording and exact source-correction allowances were repaired while every check stayed strict.

Current-owner semantic and reverse-paraphrase review:

- Non-enumerability is introduced as failure of any surjection from the positive integers: no one-way infinite list exhausts the set. The proof strategy remains to construct, from an arbitrary alleged list, an element guaranteed to be omitted.
- For `\Bin^\omega`, the target retains an alleged list `s_1,s_2,\dots` of infinite binary sequences, names coordinate `j` of row `i` by `s_i(j)`, and preserves the entire array with its emphasized diagonal entries.
- The anti-diagonal sequence `\overline{s}` flips every diagonal bit. Both the case definition and the equivalent formula `\overline{s}(n)=1-s_n(n)` are unchanged. The target separately establishes that this is an infinite binary sequence and that it differs from row `k` at coordinate `k`, so it cannot occur anywhere on the alleged list.
- The contradiction conclusion retains all quantifier dependencies: the omitted sequence is constructed from whichever complete list is assumed, not asserted independently of the list. Diagonalization is then described as a method that need not depend on a literally drawn table.
- For `\Pow{\PosInt}`, the target preserves the arbitrary alleged list `Z_1,Z_2,\dots` and the exact diagonal subset `\overline{Z}={n\in\PosInt : n\notin Z_n}`. For each `k`, membership is flipped at `k`, so `\overline{Z}\ne Z_k`. The subsequent array illustration retains all entries, gaps and the include/exclude examples 1 through 4.
- The two exercises retain diagonal arguments for `\Pow{\Nat}` and for all functions from positive integers to positive integers, including the requested explicitly omitted function `\overline f`.
- The reduction section preserves the proof direction: a surjection from `A` to `B` transforms an enumeration of `A` into one of `B`. Thus enumerability of the positive-integer power set would imply enumerability of infinite binary sequences, contradicting the preceding theorem. The related injection exercise preserves the contrapositive transfer.
- Authoritative OLSIZ-004 is applied exactly. The characteristic sequence assigned by `f(Z)` is consistently named `s`; the component clauses `s(n)=1` iff `n\in Z` and `s(n)=0` otherwise bind every symbol. An adjacent Punjabi note records replacement of the source's unbound `s_k`/`s_k(n)`, and frozen source bytes remain unchanged.
- The examples mapping the even positive integers, the empty set and all positive integers to `010101\dots`, `0000\dots` and `1111\dots` remain unchanged. The surjectivity proof reconstructs the subset of precisely those positions where an arbitrary binary sequence has value 1 and verifies `f(Z)=s`.
- The warning about reduction direction is intact: a surjection from an uncountable domain to an arbitrary `B` does not make `B` uncountable, as the first-bit map onto the finite binary set shows. Surjectivity of the reduction map is required so an enumeration of its domain cannot miss codomain elements.
- Authoritative OLSIZ-005 repairs the counterexample's codomain. Instead of the source's finite string of `n` zeros, the target displays `n` zeros followed by an infinite tail of ones, which is genuinely in `\Bin^\omega` while still giving only a narrow, non-surjective family. The additional `\dots` token is the sole command-inventory delta and is explicitly allowed by the auditor; an adjacent Punjabi note discloses it.
- The six closing exercises retain their exact targets: sets of positive-integer pairs, all natural-to-natural functions, infinite natural sequences, total versus partial zero-valued functions, surjective positive-integer-to-binary functions, and the reals.
- All `!!{...}` lexical tokens, protected TeX identifiers, formulas and arrays are unchanged except for OLSIZ-004/005's narrowly specified deltas. No ordinary English exposition remains. No integrated reader has been rendered or visually accepted.

PNB-T037 through PNB-T039 cover non-enumerability/diagonalization, the two anti-diagonal constructions, and reduction/characteristic-sequence/infinite-tail language. PNB-P007/P015 were freshly read before drafting and support prose syntax only; no specialized native attestation, dictionary acquisition or independent human review is claimed. Source translation, authoritative OLSIZ-004/005 handling and semantic review PASS.
