# BATCH-0031 source-aligned review

OLP-0067 `proof-systems/tableaux.tex`: source SHA-256 `8c9db50bcbc7a4aa757b8d57796b2e51df04b1cabb61f46756a79f72a1780b8f`; target 5,536 bytes, SHA-256 `c3aca63b6d1adb2a7397583b75cbd39b3fa0307705ed59e03bde37696fb2daa7`. OLP-0068 `proof-systems/axiomatic-deduction.tex`: source SHA-256 `b9637a6a1cee29ebba2f8d2cdf238d4567d8ec4b7995607abcb0ee1aef0570b9`; target 5,834 bytes, SHA-256 `78597c1a2dbb820ee5db25d86aa9b299bfab1fdffff09127d5b7902dacb80c83`. Respectively 11 and 11 aligned blocks; final strict audit `work/batch-0031-audit.json`, 2,079 bytes, SHA-256 `97af227cc11b0b035f9bbcf76d203110e3bdb10e4bcc7d076683d680265387f6`, passes every structural check. Predrafting consultation `work/BATCH-0031-CONSULTATION.json`, 6,097 bytes, SHA-256 `f07a094f839b8ccbd307c1fb71b0ca1f052ff1c69979bc9827591f716258cfbb`. No source correction is applied.

Current-owner semantic and reverse-paraphrase review:

- Both sections retain the exact shared `FOL` branch structure: the true branch identifies the first-order file and the false branch the propositional file. The `tab` and `axd` identifiers and all protected path arguments are unchanged, so the Punjabi prose remains one shared semantic translation under either importing part.
- The tableaux opening still contrasts derivation systems that arrange sentences with tableaux that operate on signed formulas. A signed formula remains exactly a pair consisting of a truth-value sign, either `\True` or `\False`, and a sentence; neither component has been weakened into optional metadata.
- A tableau remains a downward-branching tree of signed formulas. It begins with assumptions and continues by applying an inference rule to a signed formula above the new one. The target does not reverse the tree, turn assumptions into conclusions or permit a rule to depend only on a later node.
- The branch-extension alternatives remain distinct: a rule may append one or more signed formulas to one branch, or append two signed formulas side by side so that one branch splits into two and the added formulas terminate the new branches. “Side by side” has not been rendered as two successive formulas on the same branch.
- A rule applied to a complex signed formula still adds immediate subformulas, and the rules still come in sign-paired versions. The true-conjunction rule appends both `\sFmla{\True}{!A}` and `\sFmla{\True}{!B}` on the same branch containing `\sFmla{\True}{!A \land !B}`.
- The false-conjunction rule still splits a branch with `\sFmla{\False}{!A}` and `\sFmla{\False}{!B}` side by side. The source's unusual exact rule-name macro `\TRule{\False}{!A \land !B}` is preserved rather than normalized to a connective-only argument.
- Closure retains its universal scope: a tableau is closed only when every one of its branches contains a matching pair consisting of the same formula signed both True and False. The Punjabi does not call a tableau closed merely because some branch closes.
- Tableau derivability preserves all signs and quantifiers. `\Gamma \Proves !A` requires some finite `\Gamma_0 = \{!B_1, \dots, !B_n\} \subseteq \Gamma` and a closed tableau whose assumptions contain the goal signed False and every selected premise signed True. The finite set is not required to equal all of `\Gamma`.
- The complete `oltableau` example is command-, formula- and justification-exact. It begins with the conditional signed False, applies the false-conditional labels exactly as supplied, appends the conjunction signed True, reaches matching signed occurrences of `\formula{A}`, and still establishes `\Proves (!A \land !B) \lif !A`.
- Tableau inconsistency retains the displayed True-signed assumptions `!B_1` through `!B_n` and the source's literal closing condition “for some `!B_i \in \Gamma`.” That wording is unusual because it does not explicitly require every displayed `!B_i` to belong to `\Gamma`; the target deliberately does not strengthen it, and PNB-T098 exposes the source-scope ambiguity for expert review.
- Evert Beth and Jaakko Hintikka remain independent inventors in the 1950s; Raymond Smullyan remains the person who simplified and popularized tableaux. The target neither collapses the independent invention claim nor transfers popularization to the earlier authors.
- The practical assessment remains balanced: tableau construction is systematic and lends itself to computer implementation, while completed tableaux can be hard to read and their relation to proofs can be difficult to see. The method remains general enough for many logics.
- Model extraction keeps the full conditional chain. A satisfiable set has no closed tableau, hence any tableau has an open branch; a satisfying structure may be read from an open branch only provided every rule that can be applied on that branch has been applied. The target does not claim that every arbitrary open partial branch already determines a satisfying structure.
- The final comparison remains directional and pictorial: essentially, a closed tableau is a condensed derivation in sequent calculus written upside down. It is not identified with the whole sequent calculus or said to reverse logical consequence.
- The axiomatic-deduction opening still calls these the oldest and simplest logical derivation systems and defines their derivations as sequences of sentences. It does not import the tree shape of the preceding two systems.
- The three justification conditions remain exhaustive as listed: each sentence is an axiom, is an element of the given sentence set `\Gamma`, or is justified by an inference rule. Item order and the later references to conditions (1), (2) and (3) remain aligned.
- Axioms remain instances of a fixed collection of sentence schemas. Many schema sets may yield satisfactory sound and complete first-order derivation systems; the target does not present the following three common schemas as the only possible system.
- All three displayed schemas and their connective assignments are exact: `!A \lif (!B \lif !A)` governs the conditional, `!B \lif (!B \lor !C)` disjunction and `(!B \land !C) \lif !B` conjunction. No metavariable or connective has moved.
- Minimality retains its dependency on primitive vocabulary. Some systems minimize the number of axioms, and a single-axiom system is possible depending on which connectives are taken as primitive. The Punjabi does not assert that every choice of primitives admits the same one axiom.
- An inference rule remains a conditional statement giving a sufficient condition for a sentence in a derivation to be justified. “Sufficient” has not become necessary-and-sufficient.
- Modus ponens, rendered provisionally as `قاعدۂ اثباتِ مقدم`, retains both already-justified premises `!A` and `!A \lif !B` and the justified conclusion `!B`. At the line level, both premise formulas, for some sentence `!A`, must appear before the `!B` line; merely appearing later or elsewhere is not enough.
- Axiomatic derivability keeps its exact final-line and premise-source conditions. `\Gamma \Proves !A` requires a derivation whose last formula is `!A`, while `\Gamma` is taken as the set of sentences in that derivation justified specifically by condition (2). It is not the set of all formulas appearing in the derivation.
- Theoremhood remains the empty-`\Gamma` special case: every sentence in the derivation is justified by (1) or (3). The target does not require an empty formula sequence or silently admit condition (2).
- The three-line example is byte-for-formula exact and still proves `\Proves !A \lif (!B \lif (!B \lor !A))`. Line 1 instantiates `!A \lif (!A \lor !B)` with the roles of `!A` and `!B` reversed; line 2 instantiates `!A \lif (!B \lif !A)`; line 3 follows by modus ponens.
- The explanatory abbreviations remain correctly bound: line 3 is abbreviated as `!D`, line 2 then has form `!C \lif !D`, and `!C` is exactly line 1, `!B \lif (!B \lor !A)`. The target neither swaps `!C` and `!D` nor cites the wrong earlier line.
- Axiomatic inconsistency remains `\Gamma \Proves \lfalse`. Only a complete axiom system is then said also to prove `\lfalse \lif !A` for every `!A`, yielding `\Gamma \Proves !A` for every `!A` when `\Gamma` is inconsistent. The completeness qualification and both universal scopes are retained.
- Gottlob Frege, 1879 and `Begriffsschrift` remain associated with the first axiomatic logical systems and the first work of modern logic qualification. Alfred North Whitehead, Bertrand Russell and `Principia Mathematica`, then David Hilbert and his students in the 1920s, retain their later roles.
- “Frege systems” and “Hilbert systems” are rendered reversibly as `فریگ نظام` and `ہلبرٹ نظام` after source-spelled first mentions. The historical names are not treated as distinct formal calculi in this paragraph beyond the source's shared naming claim.
- The final tradeoff is intact: axiomatic systems are versatile and often easy to find for a logic; their simple derivation structure and one or two inference rules make results about them relatively easy to prove; but actual proof discovery and writing are very difficult. Ease of metatheoretical reasoning has not been mistranslated as ease of practical proof construction.
- Every protected identifier, tag, label, formula, tableau/proof command, lexical token, environment, command and block count is retained. No ordinary English exposition remains beyond frozen metadata comments, protected lexical-token keys, proper names and cited work titles. No reader was rendered or visually accepted for this two-unit increment.

PNB-T096 records signed formulas, truth-value signs, tableaux and downward branching. PNB-T097 records universal branch closure, open branches, the saturation qualification and model read-off. PNB-T098 records the finite-premise, False-goal and True-premise tableau definition plus the source's unusual existential inconsistency wording. PNB-T099 records axiom schemas, governed and primitive connectives, and qualified minimality. PNB-T100 records inference rules, sufficient justification, modus ponens and prior-line scope. PNB-T101 records the Frege/Hilbert names and the metatheory/practice tradeoff. Existing PNB-T086 through PNB-T091 remain applicable to proof systems, axioms, derivability, inconsistency, tableaux and sequents. PNB-P007/PNB-P015 were freshly read before drafting and support native Punjabi ordering, qualification and reason-giving syntax only; no specialized native logic attestation, dictionary acquisition or independent human review is claimed. Source translation and semantic review PASS.
