# BATCH-0024 source-aligned review

OLP-0053 dedekinds-proof.tex: source SHA-256 `a7c41cebb6b7b0e2bed0d187777be4ba69fecdb17baa070975a0ca5c5c55b8bf`; target 8,740 bytes, SHA-256 `192053703c68a60904d1a3120aae846ebd3fd1ff1009b9bbe2d18fa85576b7a9`. OLP-0054 card-sb.tex: source SHA-256 `88534a3f2be736a704ab31343e45933edb9712fa5b4411eb102c0f4a12d656e9`; target 5,836 bytes, SHA-256 `8421d718c7b88009ca9bce1377806658661df86df504da9b5fe76e14c4e8ddc0`. Respectively 15 and 18 aligned blocks; final strict audit `work/batch-0024-audit.json`, 2,108 bytes, SHA-256 `18d66536fa293a5a156116f0868142d73a57c7c2c7feac5b10ef608d7bb950d8`, passes every structural check. The first complete drafts were rejected before acceptance for reversing two inline-expression pairs, losing one piecewise row delimiter and changing one lexical injection token; all were repaired without weakening the checks.

Current-owner semantic and reverse-paraphrase review:

- The reflection opens by locating the chapter's treatment of the natural numbers in Dedekind algebras and explicitly links it with the prior discussion of the philosophical significance of arithmetizing analysis. It does not turn the technical construction into an unqualified metaphysical claim.
- The contrast with the earlier arithmetization chapter is retained: integers, rationals and reals were explicitly constructed from naturals taken as given, whereas the present result says conditionally that any Dedekind-infinite set can yield a surrogate behaving as the naturals should.
- The metaphysical-versus-mathematical distinction remains exact. Defining the reals as Dedekind cuts is described as a convenient stipulation rather than a discovery; the point is that cuts exemplify the important properties of the reals. Likewise, any Dedekind algebra exemplifies the important properties of the naturals without being declared literally identical to them.
- Dedekind's isomorphism result and its theorem-number citation are preserved as the explicit reason contemporary structuralists may regard him as a precursor. The target neither attributes the modern label to Dedekind himself nor claims that structuralism follows uniquely from the mathematics.
- The possible embedding of arithmetic into naive simple set theory remains qualified twice: that set theory is still informal and only apparently does not presuppose the naturals. Dedekind's ambitious logicist direction is presented as a possibility, not as an accomplished reduction.
- The complete attributed preface quotation retains every step: proof-worthy scientific claims should not be believed without proof; even recent foundations of elementary number theory fail the demand; arithmetic, algebra and analysis count as part of logic only in Dedekind's extended sense; and the number concept is independent of space and time and arises from pure laws of thought. The surrounding prose preserves the source's tentative “perhaps.”
- The conditional obstacle then returns explicitly. A Dedekind algebra is available only if a Dedekind-infinite set exists, so the project stalls unless such a set can be established through logic or pure thought. This prevents the earlier rhetorical “build” language from being read as an unconditional existence proof.
- Dedekind's section-66 quotation retains the proposed domain of all possible objects of thought, its name `S`, an arbitrary member `s`, the thought `s'`, the image `phi(s)` and the claimed Dedekind infinitude in their exact source order. The target preserves the quotation as Dedekind's effort rather than endorsing it.
- Both criticisms remain separate. First, the argument is unlike a modern mathematical proof because it invokes psychological and merely possible objects. Second, even if it establishes infinitely many things, it has not supplied a reason why those things form one set with infinitely many elements rather than merely a plurality.
- The terminological-historical qualification about “totality” remains attributed and footnoted. The closing paragraph then links the gap to the naive use of needed sets without general existence principles and motivates the need for such principles by Russell's Paradox. It does not itself propose an axiom system.
- The appendix restates Schröder-Bernstein with injections in both directions and a resulting bijection, cites the earlier theorem, and characterizes the new proof as naive but sophisticated. The Potter page reference, comparison with proofs of the irrationality of the square root of two and claim that many proofs exist are all retained and qualified as in the source.
- PNBINF-004 supplies the carrier scope required by the generalized closure definition and every later use: `f` is a self-map on a carrier `C`, `B` is a carrier subset and the intersected `X` values range over carrier subsets. The target states that scope in prose and an adjacent Punjabi disclosure while preserving the source's displayed intersection and all later formulas unchanged.
- The generalized closure is still exactly the intersection of all `f`-closed sets containing `B`. Its lemma retains the three source conclusions in order: it contains `B`, it is closed, and it is included in every other closed set containing `B`; its proof remains the cross-reference to the earlier point-generated closure lemma.
- The sandwich helper remains: if `A` is included in `B`, `B` in `C`, and `A` and `C` are equinumerous, the nested `cardeq` command expresses the source's chain `A approximately B approximately C`. No nested command was flattened, reordered or reinterpreted.
- Given a bijection from `C` to `A`, the helper takes the closure of `C` minus `B` and defines `g` by two exact cases: apply `f` inside that closure and use the identity outside it. The target preserves the claimed type of `g` and the composition of the inverse of `f` with `g`.
- The injection proof retains both stages. An image of a point inside the closure cannot equal the fixed value of a point outside it, because closure would force that latter point inside. Equal `g`-values then imply that both arguments lie on the same side of the case split, where either injectivity of `f` or identity gives equality.
- The range paragraph preserves the source's explicit direction: for any point of `B`, a point outside the closure is its own preimage, while a point inside the closure must be `f(y)` for some closure member, or deleting it would leave a smaller closed superset of `C` minus `B`. The other range inclusion is implicit in the source and follows immediately because all `f`-images lie in `A` inside `B`, while any input outside the closure cannot lie in `C` minus `B`. The target does not invent or reorder a displayed formula for that implicit step.
- The final proof preserves the image-set definition and the exact inclusion chain `g[f[A]]` inside `g[B]` inside `A`. The composition of the two injections is a bijection onto its defined image; the sandwich helper supplies a bijection from `A` to `g[B]`; the inverse of `g` is a bijection from `g[B]` to `B`; their composition yields the required bijection from `A` to `B`.
- Every protected identifier, section key, label, cross-reference, citation, formula, lexical token, environment, literal brace and command is retained. No ordinary English exposition remains beyond metadata comments, protected identifiers, bibliographic keys and proper names. No reader was rendered or visually accepted for this two-unit increment.

PNB-T063 records isomorphism, structuralism and the contrast between mathematical role and metaphysical identity. PNB-T064 records Dedekind's logicist direction, pure laws of thought and the plurality-to-set objection. PNB-T065 records subset-generated closure under a carrier self-map and the piecewise helper bijection under PNBINF-004. PNB-T066 records the closure/sandwich/image-chain proof of Schröder-Bernstein. PNB-P007/P015 were freshly read before drafting and support prose syntax only; no specialized native attestation, dictionary acquisition or independent human review is claimed. The fresh replay also corrected one mistyped PNB-P015 paragraph digest in the prior BATCH-0023 consultation record; the underlying canon source hash, passage text, accepted translations and historical through-52 readback receipt were unchanged. Source translation, the lane-local scope disclosure and semantic review PASS.
