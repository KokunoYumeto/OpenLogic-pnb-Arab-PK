# BATCH-0033 source-aligned review

OLP-0071 `sequent-calculus/propositional-rules.tex`: source SHA-256 `0bf253cf8afedca1478fb820770b04ab708dbe8cfd3344377a0d14ae91889276`; target 2,044 bytes, SHA-256 `98fbd8db8758877e295a0832349c5425ecf2d3c8a331f3bf087e975cf6e118df`. OLP-0072 `sequent-calculus/quantifier-rules.tex`: source SHA-256 `e0047087b88b91ba65c23a76f7c8f230c4161785f4d83f07d14461341b66d90e`; target 4,744 bytes, SHA-256 `d162b3b7ac0d0332de1641541608dc985d85368d4002fce8c2928309128dc50f`. Respectively 14 and 15 aligned blocks; final strict audit `work/batch-0033-audit.json`, 2,093 bytes, SHA-256 `fdbf2fe82b05edcd597baee3ee0563764aeff284a9e27e61821299284515ea20`, passes every structural check. Predrafting consultation `work/BATCH-0033-CONSULTATION.json`, 6,167 bytes, SHA-256 `d2df2ac2197162934b96c856c982bd597bb7d557b6f1ff99e90bbc147da8ef50`. No source correction is applied.

Current-owner semantic and reverse-paraphrase review:

- OLP-0071 retains both exact identity branches: `fol/seq/prl` under `FOL` and `pl/seq/prl` otherwise. The shared translated heading therefore remains one semantic section for both first-order and propositional builds rather than two divergent translations.
- The section and all four connective-specific headings are translated. Every formal `defish` body remains source-exact; no proof-tree prose, operand, context sequence, rule label, premise order or conclusion side was translated or reordered.
- Negation retains one unary rule on each side. Left negation moves `A` from the upper succedent to negated `A` in the lower antecedent; right negation moves `A` from the upper antecedent to negated `A` in the lower succedent.
- Conjunction retains two separate unary left alternatives, one licensed by upper `A` and one by upper `B`, and one binary right rule requiring both upper succedents. The target does not merge the left alternatives or make the right rule unary.
- Disjunction retains the dual displayed shape: one binary left rule requiring the `A` and `B` antecedent premises, plus two separate unary right alternatives. The target does not make conjunction and disjunction artificially symmetric.
- Conditional-left retains its two distinct premise contexts: the first has `A` in the succedent of `Gamma/Delta`, the second has `B` in the antecedent of `Pi/Lambda`, and the lower sequent concatenates the contexts in `Gamma, Pi` and `Delta, Lambda` order. Conditional-right retains its single premise with `A` on the left and `B` on the right.
- Every `LeftR`/`RightR` label, `UnaryInf`/`BinaryInf` choice, `A`/`B` occurrence, connective and layout command remains exact. “Left” and “right” continue to name sequent sides, not Shahmukhi page-reading direction.
- OLP-0072 retains the exact `fol/seq/qrl` identity and has no propositional fallback. The section heading and universal/existential subsection headings are translated without changing either quantifier macro.
- Universal-left retains premise `A(t), Gamma` and conclusion `forall x A(x), Gamma`; universal-right retains premise `Gamma` to `Delta, A(a)` and conclusion `Gamma` to `Delta, forall x A(x)`. Both are unary rules and remain on their source sides.
- In universal-left, `t` remains a closed term, immediately glossed as a term with no variables. The Punjabi does not weaken this to an arbitrary open term and does not identify it with the constant `a`.
- In universal-right, `a` remains formally a constant absent everywhere from the lower sequent. The source nevertheless calls it the inference’s eigenvariable and gives historical reasons; the target preserves both the formal classification and the historical terminology.
- The source’s literal `\RightR{\forall}` token in that eigenvariable sentence remains unchanged even though the displayed rule uses `\RightR{\lforall}`. This batch does not silently normalize source notation.
- Existential-left retains premise `A(a), Gamma` and conclusion `exists x A(x), Gamma`; existential-right retains premise with succedent `A(t)` and conclusion with succedent `exists x A(x)`. The same closed-`t` versus fresh-constant-`a` distinction remains attached to the correct rule pair.
- The eigenvariable condition is still precisely absence of the eigenvariable from the lower sequent of universal-right or existential-left. The target does not impose it on universal-left or existential-right.
- In the substitution explanation, `A(x)` still marks a formula in which `x` is free, while `A(t)` is shorthand for `Subst(A,t,x)`. The alternative existential-right proof tree is formal-source-exact and remains an equivalent presentation of that rule.
- Reverse-paraphrase of the occurrence qualification: in the premise `P(t,t)`, one may choose one or more occurrences of the already present `t` to correspond to bound `x` in the existential conclusion, but one need not choose all occurrences. This yields the displayed `exists x P(t,x)` example; the Punjabi does not license arbitrary textual replacement or zero replacements.
- The contrasting universal-right example remains disallowed because constant `a` already occurs in `A`: `P(a,a)` cannot support the displayed `forall x P(a,x)` conclusion using that rule. The target does not confuse this freshness failure with a malformed formula.
- The later statement that existential-right and universal-left place “no restrictions” on `t` is rendered as no *further* restriction. This keeps it compatible with the section’s explicit closed-term requirement: `t` is still variable-free, but it has no eigenvariable-style freshness condition. The interpretation is recorded in PNB-T112 rather than silently licensing open terms.
- For existential-left and universal-right, the upper-sequent restatement remains exact in scope: constant `a` may occur inside the displayed `A(a)` but nowhere outside it in that upper sequent. This is not generalized to all constants or to unrelated rules.
- Soundness remains the reason for the freshness condition, explicitly glossed as deriving only valid sequents. The target does not swap this with completeness or claim that every valid sequent is thereby derivable.
- Both starred counterexample paths remain complete and exact. One first applies the otherwise illicit existential-left step and then universal-right; the other reverses that order. Each begins from the valid identity sequent `A(a)` to `A(a)` and reaches the same existential-to-universal sequent.
- The final statement still declares only `exists x A(x)` to `forall x A(x)` invalid. It does not label the identity premise invalid, and it does not claim the implication fails for every particular interpretation of `A`.
- All protected identifiers, formulas, lexical tokens, environments, commands and aligned block counts are retained. Ordinary English remains only in frozen metadata comments and literal macro vocabulary. No reader was rendered or visually accepted for this two-unit increment.

PNB-T108 records the four propositional rule families and their exact asymmetric premise shapes. PNB-T109 records the variable-free closed-term restriction. PNB-T110 records the eigenvariable label, formal constant status, lower-sequent freshness and historical explanation. PNB-T111 records `A(x)`, `A(t)`, `Subst` and the one-or-more-but-not-necessarily-all occurrence choice. PNB-T112 records the contrast between no further restriction on closed `t` and both lower/upper formulations of eigenconstant freshness. PNB-T113 records soundness and the two-path invalid existential-to-universal counterexample. Existing PNB-T022, PNB-T067, PNB-T087, PNB-T088 and PNB-T107 remain applicable. PNB-P007/PNB-P015 were freshly read before drafting and support native Punjabi ordering, qualification and reason-giving syntax only; no specialized native logic attestation, dictionary acquisition or independent human review is claimed. Source translation and semantic review PASS.
