# BATCH-0030 source-aligned review

OLP-0065 proof-systems/sequent-calculus.tex: source SHA-256 `5448a91da184ef1d41e40ec2d56b74db63d49412d8b96b3f2a39a101b0191bad`; target 4,040 bytes, SHA-256 `9e8d550432944b58c3c353cf2732eb34ba44acc3356b01ebb49c7e7a2f59e077`. OLP-0066 proof-systems/natural-deduction.tex: source SHA-256 `77b5d31c7b63f657e0d5492d83810bfdc21af0169a9b38ccd5302e0e4cdefdfe`; target 6,712 bytes, SHA-256 `c157a1eec2c00408d0ef9efab342a961c61bfd1be9fc7bb98748522cfe4ae2cd`. Respectively 10 and 11 aligned blocks; final strict audit `work/batch-0030-audit.json`, 2,085 bytes, SHA-256 `ec62aac8e840b20f95daff05d61c641ddc8ad8206967540014d989cef716f545`, passes every structural check. No source correction is applied.

Current-owner semantic and reverse-paraphrase review:

- Both sections retain the exact shared `FOL` branch structure: the true branch identifies the first-order file and the false branch the propositional file. The `seq` and `ntd` identifiers and all protected path arguments are unchanged, so the Punjabi prose remains one shared semantic translation under either importing part.
- The sequent-calculus opening still contrasts arrangements of sentences with sequents. A sequent remains exactly the displayed two-sided expression, then a pair of sentence sequences separated by the sequent symbol. Either sequence may be empty; the target does not require both sides to contain a formula.
- The displayed general sequent preserves the source's `!A_1` through `!A_m` and `!B_1` through `!B_m` endpoints exactly. The consultation record notes that both endpoints use `m`; the target neither silently changes the right endpoint nor infers a distinct length variable.
- A sequent-calculus derivation remains a tree of sequents. Its topmost nodes are initial sequents or axioms, every other sequent follows from the ones immediately above it by an inference rule, and the later derivability definition places the relevant sequent at the root. Topmost initial nodes and the bottom proof-tree root have not been conflated.
- The two rule classes remain exhaustive as stated: rules either add, remove or rearrange sentences on the left or right, or introduce a complex formula in their conclusion. “Left” and “right” refer to the two sequent sides rather than page direction or Punjabi reading order.
- The two sample inferences are formula- and direction-exact. Left conjunction goes from `!A, \Gamma \Sequent \Delta` to `!A \land !B, \Gamma \Sequent \Delta`; right conditional goes from `!A, \Gamma \Sequent \Delta, !B` to `\Gamma \Sequent \Delta, !A \lif !B`. All four metavariables remain arbitrary, and the explicit possibility that `\Gamma` and `\Delta` are empty remains present.
- Sequent-calculus derivability keeps all three restrictions: some sequence `\Gamma_0` exists, every formula in it belongs to `\Gamma`, and a derivation has `\Gamma_0 \Sequent !A` at its root. The Punjabi does not require `\Gamma_0` to equal `\Gamma`, reverse the membership condition or place the root sequent at a leaf.
- Theoremhood remains the special empty-left sequent criterion: `!A` is a theorem when `\Sequent !A` has a derivation. The following prooftree is byte-for-command and formula exact and still establishes `\Proves (!A \land !B) \lif !A` by left conjunction followed by right conditional.
- Sequent-calculus inconsistency remains existence of a derivation of `\Gamma_0 \Sequent` with every member of `\Gamma_0` drawn from `\Gamma` and an empty right side. The target does not substitute the different natural-deduction falsity criterion into this definition. Right weakening still yields derivability of every sentence from such an inconsistent set.
- Gerhard Gentzen and the 1930s attribution are retained. The systematic and symmetric design still makes the calculus useful for theorizing about derivations; derivations remain relatively easy to find but often hard to read, their connection to proofs can be difficult to see, and the concluding positive assessment and availability for many logics remain intact.
- The frozen non-rendered comment's misspelling `seqeunt-calculus` is preserved exactly, while the rendered heading uses the intended Punjabi name. This is metadata fidelity, not a source correction.
- Natural deduction remains a derivation system intended to mirror actual reasoning, especially regimented mathematical reasoning. Its source motivation is not strengthened into an identity claim between formal derivations and every act of human reasoning.
- Proof by cases still starts from a disjunctive premise and establishes the same conclusion from each disjunct. Indirect proof establishes a conclusion by showing that its negation leads to contradiction. Conditional proof establishes an if-then claim by showing that the consequent follows from the antecedent. The three patterns and their directions remain distinct.
- Every logical connective and quantifier still has an introduction rule and an elimination rule, each corresponding to a natural inference pattern. The exact examples remain: conditional introduction corresponds to conditional proof, disjunction elimination to proof by cases, and conjunction elimination permits inference from `!A \land !B` to `!A` or `!B`.
- The distinctive role of assumptions remains central. A natural-deduction derivation is a formula tree with one formula at its root and premise formulas at its leaves. Some leaf formulas can play a temporary internal role and be used up before the conclusion, matching short-lived hypotheses in ordinary reasoning.
- The three temporary-assumption examples remain aligned with the earlier proof patterns: case proof assumes each disjunct, conditional proof assumes the antecedent, and indirect proof assumes the negation of the conclusion. None is recast as a permanent premise or as an assertion of the final result.
- Leaf formulas are still called assumptions, and inference rules may discharge them. If a derivation of `!B` uses assumptions including `!A`, conditional introduction permits inference of `!A \lif !B` and discharge of assumptions of the form `!A`. Formula order, connective and discharge scope are unchanged.
- Number labels still track which assumptions are discharged at which inference. Assumptions left undischarged at the end jointly suffice for the conclusion, so the derivation establishes entailment from those remaining assumptions. The target does not say that discharged assumptions remain premises of the result.
- Natural-deduction derivability keeps the exact leaf condition: `\Gamma \Proves !A` iff a derivation ends with `!A` and every undischarged leaf belongs to `\Gamma`. Theoremhood keeps its stronger special case: a derivation ends with `!A` and all assumptions are discharged. Neither criterion requires every member of `\Gamma` to occur.
- The natural-deduction prooftree is byte-for-command and formula exact. The assumption `!A \land !B` is marked with label 1, conjunction elimination yields `!A`, conditional introduction yields `(!A \land !B) \lif !A`, and the following sentence correctly identifies label 1 as the discharge marker at that inference.
- Natural-deduction inconsistency remains `\Gamma \Proves \lfalse`; the falsity rule then makes every sentence derivable from an inconsistent set. This preserves the same explosive consequence as the preceding survey while retaining the calculus-specific formal criterion.
- Gerhard Gentzen, Stanisław Jaśkowski, Dag Prawitz and Frederic Fitch, the 1930s development and later-development relation all remain present. The claims about philosophical preference and Fitch-style introductory textbooks retain their original scope.
- The final philosophical sentence remains qualified: natural-deduction rules have sometimes been taken, in philosophy of logic, to give the meanings of logical operators. “Proof-theoretic semantics” names that reported position; the target does not assert that every logician accepts it or that the rules universally fix meaning.
- Every protected identifier, tag, label, formula, proof-tree command, lexical token, environment, command and block count is retained. No ordinary English exposition remains beyond frozen metadata comments, protected lexical-token keys and proper names whose source spelling preserves required TeX orthography. No reader was rendered or visually accepted for this two-unit increment.

PNB-T090 records sequents, sequent calculus and the pair of possibly empty sentence sequences. PNB-T091 records initial sequents, proof-tree root orientation, left/right sides and weakening. PNB-T092 records natural deduction, proof by cases, indirect proof, conditional proof and introduction/elimination rules. PNB-T093 records premise/conclusion, antecedent/consequent and disjunct roles. PNB-T094 records assumptions, temporary use, discharge and undischarged leaves. PNB-T095 records the qualified proof-theoretic-semantics position. PNB-T086 through PNB-T089 remain applicable to axioms, rules, theoremhood, derivability and inconsistency. PNB-P007/P015 were freshly read before drafting and support native Punjabi ordering, qualification and reason-giving syntax only; no specialized native logic attestation, dictionary acquisition or independent human review is claimed. Source translation and semantic review PASS.
