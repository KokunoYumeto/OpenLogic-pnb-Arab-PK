# BATCH-0034 source-aligned review

OLP-0073 `sequent-calculus/structural-rules.tex`: source SHA-256 `5c13d5312a8b2287b54d67d2fadc6247ba499bd9b6a71255ea04891307689b06`; target 2,723 bytes, SHA-256 `302ed0953dd4ca71c8e84fbe781152d96d0d04727ee9a2f65ae900e41edbd008`. OLP-0074 `sequent-calculus/derivations.tex`: source SHA-256 `25897d850b211443621f31a1166f443c1e00c6bcbf13002bc1b9281ca4300371`; target 5,553 bytes, SHA-256 `e105b19a1ec4e02d49dcdf49c218d704977275993c4bb3e4d3c64cfd9b91ad28`. Respectively 15 and 9 aligned blocks; final strict audit `work/batch-0034-audit.json`, 2,083 bytes, SHA-256 `c86fa57e9a5780296725ed23959be320111488b4e5021151f9b8c7133f180cde`, passes every structural check. Predrafting consultation `work/BATCH-0034-CONSULTATION.json`, 6,525 bytes, SHA-256 `28d8214108b29c6c36d100329defaf9845d199995ef8528b9f198d53880b01e5`. No source correction is applied.

Current-owner semantic and reverse-paraphrase review:

- OLP-0073 retains both exact file-identity branches: `fol/seq/srl` under `FOL` and `pl/seq/srl` otherwise. Its structural rules therefore remain shared by the first-order and propositional builds rather than being duplicated or made first-order-only.
- The opening motivation still distinguishes structural from logical rules. Logical rules require the sentence they act on at the far left or far right of a sequent side; structural rules make the required reordering and multiplicity changes possible. The Punjabi `کھبا/سجا سرا` continues to name abstract sequence endpoints, not RTL page direction.
- Exchange is motivated only as moving sentences to the required position. The target does not say it moves a sentence across the sequent divider, changes its truth role or replaces it by another sentence.
- The two other operations remain separate: Contraction combines two identical sentence occurrences into one, while Weakening adds a sentence on either side. The target does not describe Contraction as arbitrary deletion or Weakening as substitution.
- Both Weakening schemata remain formal-source-exact. Left Weakening adds `A` before `Gamma` in the antecedent; right Weakening adds `A` after `Delta` in the succedent. Each has one premise and retains both surrounding sequences.
- Both Contraction schemata remain exact. The relevant premise contains two adjacent copies of the same `A`, and its conclusion contains one copy on that same side. No nonidentical pair is contracted.
- Both Exchange schemata remain exact. On the left, `Gamma, A, B, Pi` becomes `Gamma, B, A, Pi`; on the right, `Delta, A, B, Lambda` becomes `Delta, B, A, Lambda`. The outer contexts, side and adjacency are unchanged.
- `کمزور کرنا`, `سکیڑنا` and `تبادلہ` are provisional, transparent Punjabi rule labels. Literal `Weakening`, `Contraction` and `Exchange` macros remain unchanged in every display, so expert reviewers can recover the exact imported terminology without treating these labels as settled native attestation.
- Double inference lines still abbreviate a series of Weakening, Contraction and Exchange inferences. The Punjabi preserves all three rule families, plurality and the “often indicated” convention; it does not introduce double lines as a fourth primitive structural rule.
- Cut remains qualified as not strictly necessary, while making reuse and combination of derivations much easier. The target does not assert or prove cut elimination in this section.
- The Cut schema remains exact and binary. `A` is in the first premise’s succedent and the second premise’s antecedent; the conclusion omits it and concatenates `Gamma, Pi` on the left and `Delta, Lambda` on the right in that order.
- OLP-0074 retains both file-identity branches: `fol/seq/der` under `FOL` and `pl/seq/der` otherwise. The heading keeps the source `\usetoken{P}{derivation}` lexical token rather than hard-coding a competing title.
- The informal inductive account remains exhaustive: a derivation is either an initial sequent by itself or consists of one or two derivations followed by an inference. “One or two” is not weakened to an arbitrary number and the inference remains below/following its premises.
- The formal definition retains finiteness and all three tree conditions. Topmost nodes are initial sequents; the bottommost node is the designated sequent `S`; every node other than `S` is a premise of a correct rule application whose conclusion stands immediately below it.
- The first strict audit rejected a Punjabi genitive construction because it put formula `S` before `LK`, reversing the source’s mathematical-token sequence. The repaired sentence begins with the `LK` derivation and then identifies `S`, preserving natural Punjabi while restoring exact formula order. No audit exception was added.
- `S` remains the end-sequent of the derivation, and both “derivable in LK” and “LK-derivable” formulations remain present. Derivability is not replaced by semantic validity.
- The worked example begins from the initial sequent `C` to `C`. Its first generic left-Weakening display remains exact, then the prose instantiates `A` by `D`, with both `Gamma` and `Delta` the singleton sequence `C`, producing `D, C` to `C`.
- The next displayed derivation adds left Exchange and yields `C, D` to `C`. The target preserves the order of the two antecedent sentences and does not use exchange to cross the sequent divider.
- The generic Exchange display in the source has a literal comma inside the concluding math expression after `Delta`. That punctuation remains unchanged; this batch records no source defect or silent formal cleanup.
- In the explicit Exchange-role account, both `Gamma` and `Pi` are empty, `Delta` is `C`, and the roles of `A` and `B` are played respectively by `D` and `C`. Every metavariable and concrete formula occurs in source order; “respectively” is retained.
- The parallel one-step derivation from `D` to `D` by left Weakening still yields `C, D` to `D`. This gives a second last sequent with exactly the same antecedent as the preceding `C, D` to `C` derivation.
- Right conjunction is applied only after the two last sequents match the two premises of its generic rule. `Gamma` is `C, D`, `Delta` is empty, `A` is `C` and `B` is `D`; the resulting conclusion is exactly `C, D` to `C` conjunction `D`.
- All eight `prooftree` bodies remain byte-for-byte identical to the frozen source. This includes each axiom, label, unary/binary inference, formula, comma and sentence order; prose alone is translated.
- The final example reverses the binary premises. Consequently `A` is `D` and `B` is `C`, so the displayed conclusion is `D` conjunction `C`, not normalized back to `C` conjunction `D`. The common antecedent remains `C, D`.
- That final contrast is syntactic, not a claim that conjunction is semantically noncommutative. The exact proof trees govern the premise-to-conjunct correspondence, while the review record leaves the register and labels open to correction.
- Every protected identifier, lexical token, formula, environment, command and aligned block count is retained. Ordinary English remains only in frozen metadata comments and literal TeX vocabulary. No reader was rendered or visually accepted for this two-unit increment.

PNB-T114 records the structural-rule role and endpoint motivation. PNB-T115, PNB-T116 and PNB-T117 record Weakening, Contraction and Exchange with their exact add, duplicate-collapse and within-side swap behavior. PNB-T118 records the double-inference-line convention. PNB-T119 records Cut, its qualification, binary schema and reuse/combine rationale. PNB-T120 records inductively generated finite LK derivations, tree orientation, end-sequents and derivability. PNB-T121 records rule-schema premise matching, metavariable roles and empty contexts. PNB-T122 records binary right-conjunction combination and premise-dependent conjunct order. Existing PNB-T022, PNB-T087, PNB-T090, PNB-T091, PNB-T105 through PNB-T108 remain applicable. The occurrence matcher explicitly suppresses unrelated relation-order, Dedekind-cut, mathematical-induction, graph-tree, formula-formation and empty-side-semantic decisions in OLP-0073/0074. It also activates PNB-T108–T113 only from OLP-0071 and PNB-T114–T122 only from OLP-0073, preventing later phrase matchers from silently rewriting already verified review locations. PNB-P007/PNB-P015 were freshly read before drafting and support native Punjabi ordering, qualification and reason-giving syntax only; no specialized native logic attestation, dictionary acquisition or independent human review is claimed. Source translation and semantic review PASS.
