# BATCH-0032 source-aligned review

OLP-0069 `sequent-calculus/sequent-calculus.tex`: source SHA-256 `f7e79d7b3f181c61d3defb24c630db20ceaf12a3ec237c9b0a2ab1f7855494fb`; target 1,201 bytes, SHA-256 `a838b79fcc63958e48e56d79751a6641a80c3e206597e26a8dccfabb6fbffed7`. OLP-0070 `sequent-calculus/rules-and-proofs.tex`: source SHA-256 `73a554964d7a3fc35a116f63e658340a8c89f9915c3f27e23f7ebd762df35859`; target 4,190 bytes, SHA-256 `e77c111773757d8cc1d9120ffc5868c9140b79ecde994b5ce09f5455dc106227`. Respectively 20 and 12 aligned blocks; final strict audit `work/batch-0032-audit.json`, 2,090 bytes, SHA-256 `1d0545bfb6498d5614d4df2e9d24ce280a4e79d729251113dde36b183dc1cee2`, passes every structural check. Predrafting consultation `work/BATCH-0032-CONSULTATION.json`, 5,735 bytes, SHA-256 `894b72a1ca02ffb0f7d3474d7aae7849f52ff8950b08c35ddf93ef0fc1e500ea`. No source correction is applied.

Current-owner semantic and reverse-paraphrase review:

- The chapter driver retains both exact identity branches. Under `FOL` it opens `fol/seq`; otherwise it opens `pl/seq`; both rendered chapter titles say Sequent Calculus. The target does not collapse the branch or invent different chapter names.
- The editorial still identifies the presented system as Gentzen's standard sequent calculus `LK` for classical first-order logic. Although the driver also has a propositional identity branch, the source editorial itself states first-order scope without a local tag; the target preserves that statement rather than silently generalizing it.
- The request for more examples and exercises remains an editorial deficiency, not a claim that such material has now been added. The Punjabi phrase says the chapter still needs them.
- Literal tag `prfLK` remains unchanged and is still the mechanism for including or excluding material relevant to sequent calculus as a proof system. It is not rendered as a logical connective, renamed or made equivalent to the surrounding `FOL` tag.
- The complete import graph is path- and order-exact. `rules-and-proofs` precedes `propositional-rules`; then the driver proceeds through structural rules, derivations and proof construction, proof-theoretic notions, provability and consistency, propositional provability, soundness and the final chapter hook.
- Every first-order-only gate remains bounded as in the source. `quantifier-rules`, `proving-things-quant` and `provability-quantifiers` each have their own `FOL` conditional; `identity` and `soundness-identity` share the final `FOL` conditional. No conditional import was made unconditional and no common import was hidden behind `FOL`.
- In the section, both exact file-identity branches remain: `fol/seq/rul` under `FOL` and `pl/seq/rul` otherwise. The protected identifiers and translated heading preserve its shared first-order/propositional role.
- `\Gamma`, `\Delta`, `\Pi` and `\Lambda` still range over finite sequences of sentences. The target does not treat them as sets, single sentences or necessarily nonempty sequences.
- A sequent remains exactly the displayed expression `\Gamma \Sequent \Delta`. Both sides are finite, possibly empty sequences of sentences in language `\Lang L`; the possibly-empty qualification applies to each side.
- The left sequence `\Gamma` is provisionally called `مقدم`, and the right sequence `\Delta` `تالی`. These names denote whole sequences here, not merely the antecedent and consequent formulas of a single conditional; the adjacent display and definition govern their scope.
- The intuitive reading retains different quantifiers on the two sides: all sentences in the antecedent must hold, while at least one sentence in the succedent must hold. The Punjabi does not require every succedent sentence and does not weaken the antecedent to one member.
- The tuple endpoints and variable roles remain exact. `\Gamma` contains `!A_1` through `!A_m`, `\Delta` contains `!B_1` through `!B_n`, and the corresponding sentence is the conditional from the conjunction of all `A` formulas to the disjunction of all `B` formulas. `m` and `n` are not interchanged.
- The general equivalence remains biconditional: the sequent holds iff the displayed conditional holds. The target uses the established explicit `اُدوں تے صرف اُدوں ... جدوں` construction and preserves connective direction.
- The two empty-side cases remain distinct. If `\Gamma` is empty (`m = 0`), the sequent with an empty left side holds iff the succedent disjunction holds. If `\Delta` is empty (`n = 0`), the sequent with an empty right side holds iff the negation of the antecedent conjunction holds.
- The target does not turn either empty side into an empty set, omit the visible spacing commands or infer a truth/falsity constant not supplied by this paragraph. Every displayed and inline formula remains in source order.
- A sequent remains valid iff its corresponding sentence is valid. `معتبر` is used as a reversible scholarly bridge, controlled here by the immediately preceding correspondence rather than by a new independent semantics.
- Sequence append direction is exact. `\Gamma, !A` appends `!A` to the right end of `\Gamma`; `!A, \Gamma` appends it to the left end. “Right” and “left” name abstract sequence endpoints and are not reversed to follow RTL page direction.
- `\Gamma, \Delta` remains concatenation of the two sequences in Gamma-then-Delta order. The Punjabi `دوناں سلسلیاں دا جوڑ` does not make concatenation commutative; the unchanged notation fixes the order.
- The initial-sequent definition preserves its complete conditional structure. In both branches `!A \Sequent !A` is the core form for any sentence `!A` in the language.
- Only the `\iftag{prvFalse,prvTrue}` branch opens the enumeration. Within it, the `prvTrue` item is the empty-left `\Sequent \ltrue` form and the `prvFalse` item is the empty-right `\lfalse \Sequent` form. Their signs, sides, tags and empty positions are unchanged.
- The fallback branch still gives only the identity form. The target does not make the tagged truth or falsity initial sequents available in a build lacking their respective tags.
- Sequent-calculus derivations remain certain trees of sequents. Topmost sequents are initial sequents, and a sequent below one or two others must follow correctly from those upper sequents by an inference rule. The target neither reverses tree orientation nor changes the allowed premise count.
- `\Log{LK}` rules remain divided into two main types, logical and structural. `ساختی` is a provisional transparent bridge and is not used to erase the formal distinction between the two types.
- Logical rules are still named for the main operator of the sentence containing `!A` and/or `!B` in the lower sequent. The target retains the lower-sequent conclusion orientation and does not choose the rule name from an arbitrary upper premise.
- Each logical rule still has two versions: one infers a sequent whose operator-containing sentence is on the left, and one whose such sentence is on the right. Both sides refer to sequent position, not Shahmukhi reading direction.
- Every protected identifier, import, tag, formula, lexical token, environment, command and block count is retained. Ordinary English remains only in frozen metadata comments, literal identifiers and the proper name `Gentzen`. No reader was rendered or visually accepted for this two-unit increment.

PNB-T102 records named `LK`, its classical first-order scope, literal `prfLK` selection tag and the prospective editorial limitation. PNB-T103 records finite possibly empty sequent sides and the provisional `مقدم`/`تالی` labels. PNB-T104 records the all/at-least-one reading, both empty-side cases and sequent validity. PNB-T105 records endpoint-sensitive append and ordered concatenation. PNB-T106 records the unconditional identity initial sequent and the two tag-controlled truth/falsity forms. PNB-T107 records logical versus structural rules, lower-sequent main-operator naming and left/right versions. Existing PNB-T087, PNB-T090, PNB-T091 and PNB-T093 remain applicable. PNB-P007/PNB-P015 were freshly read before drafting and support native Punjabi ordering, qualification and reason-giving syntax only; no specialized native logic attestation, dictionary acquisition or independent human review is claimed. Source translation and semantic review PASS.
